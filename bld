#!/usr/bin/env bash
# ./bld2 -o outfile compiler pkgdir
set -eux
shift
OUTFILE=$1
compiler=$2
pkgdir=$3

workdir=${OUTFILE}.d
export WORKDIR=$workdir
mkdir -p $workdir
$compiler list -deps $pkgdir  > $workdir/list

./build.sh -o $OUTFILE $compiler $pkgdir
